'use strict';

angular.module('mean.bukasystem')
    .controller('BukasystemController', ['$scope', 'Global', 'Bukasystem',
        function ($scope, Global, Bukasystem) {
            $scope.global = Global;
            $scope.package = {
                name: 'bukasystem'
            };
        }
    ])
    .controller('ProductController', ['$scope', 'Global', 'Bukasystem',
        function ($scope, Global, Bukasystem) {
            $scope.global = Global;
            $scope.package = {
                name: 'bukasystem'
            };
        }
    ])
    .controller('CaseController', ['$scope', 'Global', 'Bukasystem',
        function ($scope, Global, Bukasystem) {
            $scope.global = Global;
            $scope.package = {
                name: 'bukasystem'
            };
        }
    ])
    .controller('PriceController', ['$scope', 'Global', 'Bukasystem',
        function ($scope, Global, Bukasystem) {
            $scope.global = Global;
            $scope.package = {
                name: 'bukasystem'
            };
        }
    ])


    .controller('DocsController', ['$scope', 'Global', 'Bukasystem',
        function ($scope, Global, Bukasystem) {
            $scope.global = Global;
            $scope.package = {
                name: 'bukasystem'
            };
        }
    ])
    .controller('AboutController', ['$scope', 'Global', 'Bukasystem',
        function ($scope, Global, Bukasystem) {
            $scope.global = Global;
            $scope.package = {
                name: 'bukasystem'
            };
        }
    ])
    .controller('ConsoleController', ['$scope', 'Global', 'Bukasystem',
        function ($scope, Global, Bukasystem) {
            $scope.global = Global;
            $scope.package = {
                name: 'bukasystem'
            };
        }
    ]);
